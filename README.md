# GPT-4 Assistant with a Persistent Memory
A simple assistant that has a persistent memory based on reading and writing from the filesystem.
Requires GPT-4 to work well.
```python remember.py --notes-dir ./notes```